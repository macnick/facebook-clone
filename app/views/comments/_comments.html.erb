<% if !@comments.nil? %>
    <% @comments.each do |c| %>
    <div class="container">
        <div class="p-3 mb-3 bg-primary rounded col-md-8 offset-md-2 text-white">
            <%= c.comment_text %>
        </div>
    </div>
    <% end %>
<% end %>